from achatbot.cmd.http.server import fastapi_daily_bot_serve as serve

bot_join = serve.bot_join
create_random_room = serve.create_random_room
createroom = serve.create_room
bot_join = serve.get_status
